package ch.riesennet.reforge.operation

/**
 * Base interface for typed operation specifications.
 * Each operation type defines its own spec class implementing this interface.
 */
interface OperationSpec
